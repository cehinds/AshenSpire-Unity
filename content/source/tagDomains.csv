# content/source/tagDomains.csv — what a tag can be ABOUT (Ashen Spire)
#
# The authoritative home for the domain vocabulary. Every tags.csv row names one
# of these, and every tagFamilies row draws from a subset of them
# (tagFamilyDomains.csv).
#
# WHY ITS OWN TABLE: `domain` on a tag used to be a bare string whose legal
# values were inferred from the union of every family's domain list. That made
# the vocabulary a derived fact with no home — a typo could invent a domain, and
# a domain with no tags was indistinguishable from one nobody had authored yet.
# It is a lookup table now, and both carriers are ordinary foreign keys into it.
#
# A domain is not a tag. It never appears on a card and is never displayed; it
# exists so ONE registry can hold every word in the game without making every
# word legal everywhere.
#
# columns: id, label, blurb
id,label,blurb
card,Card,"What a card IS — its school, form and feel. Carried by cards, by the classes that lean on them, and by the equipment that rewrites them."
creature,Creature,"What a creature IS. Gates proc resistance, and the one domain the engine reads at runtime."
item,Item,"What a piece of gear DOES beyond its numbers, and what a run pickup is."
run,Run,"Run structure — what an event, an encounter or an unlock is about."
itemType,Item type,"What a piece of equipment IS, as the Armoury and the smith name it. Distinct from the card domain: a Blade item type and a Blade card school are different facts about the same sword."
grantSource,Grant source,"Where a starting card came from. The composed deck deals bound cards in this vocabulary's authored order, so adding a source is a row here rather than a code change. Nothing WEARS these — the `grant` family exists to declare them, the way `effect` declares which words a damage effect may carry."

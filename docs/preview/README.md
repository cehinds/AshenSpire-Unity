# docs/preview

Only captures referenced elsewhere in the repo (root `README.md` links, `docs/*.md`
evidence, etc.) are tracked here. Ad-hoc screenshot runs are throwaway — regenerate
them with the relevant `tools/*.mjs` capture script rather than committing them.
